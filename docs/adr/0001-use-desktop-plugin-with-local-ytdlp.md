# Use Desktop Plugin With Local yt-dlp

Video To Obsidian is an Obsidian desktop plugin that invokes the user's configured local `yt-dlp` executable instead of using a web app, mobile-first flow, or hosted transcript service. This keeps transcript acquisition under the user's local setup and avoids operating a companion backend, at the cost of requiring desktop Obsidian and explicit executable configuration.
